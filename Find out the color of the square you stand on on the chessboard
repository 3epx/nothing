#include <iostream>
using namespace std;
int main() {
    
    int  x = 0 , y = 0;
    cout << "enter the numbers x then y : "; // 1 = black , 0 = white
    cin >> x >> y;
    x = x+y;
    bool result = (x %2 == 0); 
    cout << result;
    return 0;
}
