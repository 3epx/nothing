#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main() {

	ifstream file("example.txt");
	if (file.is_open()) {
		string line;
		int n = 1; 

		while (getline(file, line)) {
			cout << "the line : " << n << " contain of : " << ahmed << endl;
				n++;
		}
		file.close();

	}
	else { cout << "the file is non"; }
}
