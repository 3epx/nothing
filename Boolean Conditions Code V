#include <iostream>
using namespace std;
int main() {
    
    int a = 0, b = 0;
    cout << "enter the number in paralell a then b : ";
    cin >> a >> b;
    bool result = (a %2 == 1) && (b %2 == 1);
    cout << "the number is : " << result;
    
    return 0;
}
