#include <iostream>
using namespace std;
int main() {
    int x[10] = {2,3,4,5,6,7,3,12,3,33};
    int y[10] = {2,2,4,5,6,1,2,2,2,2};
    int h = 0;
    for (int i = 0 ; i<10; i++){
        for (int u = 0; u <10 ; u++){
            if (x[i] == y[u]){h++;}
        }
        if (h == 0){cout << "the x araay = " << x[i] << endl;}
        h= 0;
    }
    return 0;
}
