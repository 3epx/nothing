#include <iostream>
using namespace std;
int main() {

  
    int x = 0, a = 0, y = 0 , s = 0 , as = 0;
    cout << "enter the amount of a in kg: ";
    cin >> x; cout << endl;
    cout << "enter the price of this amount : ";
    cin >> a; cout << endl;
    cout << "enter the amount of choclate to calc it's price : ";
    cin >> y; cout << endl;
    a = a / x;
    cout << "the price of the sweet have " << y << "kg is " << a * y << endl;
    cout << "enter the amount of a in kg: ";
    cin >> s; cout << endl;
    cout << "enter the price of this amount : ";
    cin >> as; cout << endl;
    cout << "enter the amount of sugar to calc it's price : ";
    cin >> y; cout << endl;
    as = as / s;
    a = a - as;
    if (a < 0) { a = a * -1; }
    cout << "the defrance of price between choclate is " << a;



    return 0;

}
