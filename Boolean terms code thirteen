#include <iostream>
using namespace std;
int main() {
    
    int a = 0; 
    cout << "enter the numbers pls : ";
    cin >> a;
    float b = a/100;
    bool result = (b >= 1) && (a %2 == 1); 
    cout << result; 
    
    
    return 0;
}
