#include <iostream>
#include <cmath>
using namespace std;

int main (){
    
    int  x = 0 , y = 0;
    cout << "enter the numbers pls : ";
    cin >> x >> y;
 
    float result = (x > 0) and (y > 0) or  (x < 0) and (y < 0);
    cout << result;


    return 0;
}
