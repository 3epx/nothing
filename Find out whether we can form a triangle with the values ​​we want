#include <iostream>
using namespace std;
int main() {
    
    int a = 0 , b = 0 , c = 0;
    cout << "enter the numbers a then b then c : ";
    cin >> a >> b >> c;
    
    bool result = (c < a + b) && (b < a + c) && (a < c + b);
    cout << result;
    return 0;
}
