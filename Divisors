#include <iostream>
using namespace std;
int main() {
    
    int number = 0;
    cout << "enter the number pls :";
    cin >> number;
    
    for (int i = 1; i <= number; i++){
        if (number %i == 0){cout << i << endl;}
        else {continue;}
    }
   
    return 0;
}
