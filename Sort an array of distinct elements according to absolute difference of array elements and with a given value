#include <iostream>
using namespace std;
int main() {
    int x[10] = {1,2,4,3,2,1,23,1,4,1};
    int y[10];
    int z[10];
for(int i = 0;  i <10; i++){
    int r = 9 - x[i];
    if (r < 0){ r = r * -1;}
    y[i] = r;
}
int max;
for(int i = 0 ; i<10 ; i++){
    if (max < y[i]){max = y[i];}
}
int kk = max;
int yy = 9;
while (true){
    if (yy = 0){break;}
    for(int i = 0 ; i<10 ; i++){
    if (kk-1 == y[i])
        cout << kk - 1<< endl;
    }
    kk--;
    yy--;
}

    return 0;
}
