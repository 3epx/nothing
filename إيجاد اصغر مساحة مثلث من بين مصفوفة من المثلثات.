#include <iostream>
using namespace std;
int main() {

	float rect[5] = { 5 * 3,4 * 6,3 * 2,1 * 2,7 * 8 };
	int min = rect[0];

	for (int i = 0; i < 5;i++) {
		if (rect[i] < min) {
			min = rect[i];
		}
	}
	cout << "mim area is " << min;

}
